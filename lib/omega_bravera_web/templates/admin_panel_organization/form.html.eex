<%= form_for @changeset, @action, [id: "organization-form"], fn f -> %>
    <%= if @changeset.action do %>
    <div class="alert alert-danger">
        <p><%= gettext("Oops, something went wrong! Please check the errors below.") %></p>
    </div>
    <% end %>

    <div class="form-group">
        <%= label f, :name, class: "control-label" %>
        <%= text_input f, :name, class: add_error_class(f, :name, "form-control") %>
        <%= error_tag f, :name %>
    </div>

    <div class="form-group">
        <%= label f, :business_type, class: "control-label" %>
        <%= text_input f, :business_type, class: add_error_class(f, :business_type, "form-control") %>
        <%= error_tag f, :business_type %>
    </div>

    <div class="form-group">
        <%= label f, :business_website, class: "control-label" %>
        <%= text_input f, :business_website, class: add_error_class(f, :business_website, "form-control") %>
        <%= error_tag f, :business_website %>
    </div>

<div class="mb-3">
        <%= submit gettext("Submit"), class: "btn btn-primary" %>
    </div>
<% end %>
